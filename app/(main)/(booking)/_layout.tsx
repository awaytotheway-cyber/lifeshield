import { Stack } from "expo-router";

/** Guided test booking: where → lab → time → prepare → confirmed. */
export default function BookingLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
