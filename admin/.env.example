# Copy to admin/.env and paste the SAME values as your main LifeMate .env
# (only the URL and anon key — never the service-role key).
# After saving, restart: npm run dev

VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=paste_your_anon_public_key_here
